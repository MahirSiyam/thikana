/**
 * Mock data for the admin Reports & Analytics page has been retired.
 *
 * The page is now data-driven via `getAdminReportsAnalytics()` from
 * `@/lib/api/admin`. This file remains so any historical import paths still
 * resolve, but the live arrays are intentionally empty.
 */

export {};
